import React from 'react';

const Header = () => {
    return (
        <header>
            <h1>
                <i className="fa fa-book"></i>
                Book Cards
            </h1>
        </header>
    );
};

export default Header;